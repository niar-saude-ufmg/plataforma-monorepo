import '@testing-library/jest-dom/vitest'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { Loading, LoadingSkeleton } from './Loading';
describe('Loading', () => { it('expõe o status de carregamento', () => { render(<Loading label="Carregando" />); expect(screen.getByRole('status')).toHaveTextContent('Carregando'); }); it('renderiza as linhas do skeleton', () => { render(<LoadingSkeleton lines={3} />); const status = screen.getByRole('status', { name: 'Carregando conteúdo' }); expect(status).toHaveAttribute('aria-busy', 'true'); expect(status.children).toHaveLength(3); }); });
