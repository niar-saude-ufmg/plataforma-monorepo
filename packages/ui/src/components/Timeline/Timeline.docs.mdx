import {
  Canvas,
  Controls,
  Description,
  Meta,
  Source,
} from "@storybook/addon-docs/blocks";
import * as Stories from "./Timeline.stories";

<Meta of={Stories} />

# Timeline

Linha do tempo para exibir o histórico de mudanças de um projeto.

<Description of={Stories.Playground} />
<Canvas of={Stories.Playground} />
### Props
<Controls of={Stories.Playground} sort="alpha" />

Cada item pode definir `color` como `primary`, `secondary`, `success`, `warning`, `error` ou `grey`.

### Cores

<Description of={Stories.Colors} />
<Canvas of={Stories.Colors} />

### Compact
<Description of={Stories.Compact} />
<Canvas of={Stories.Compact} />
### Exemplo de aplicação
<Source
  dark
  language="tsx"
  code={`import { Timeline } from "@niar/ui";

export function History() {
    return (
      <Timeline
        items={[{ title: "Projeto submetido", date: "Hoje, 10:30" }]}
      />
    );
}`}
/>
### Acessibilidade - Mantenha os eventos em ordem cronológica. - Use títulos e
datas compreensíveis.
